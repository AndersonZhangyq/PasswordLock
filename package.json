{
  "productName": "PasswordLock",
  "name": "passwordlock",
  "version": "0.1.0",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "package-windows": "electron-packager . PasswordLock --overwrite --asar=true --platform=linux --arch=x64 --prune=true --out=release-builds"
  },
  "devDependencies": {
    "electron": "^1.8.2"
  }
}
